package com.amadeus.session.sample;

import javax.servlet.Filter;

/**
 * Filter that has super class and implements Filter interface.
 */
public class DerivedAndImplementsFilter extends AbstractFilter implements Filter {
}
